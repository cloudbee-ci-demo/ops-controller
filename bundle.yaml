apiVersion: "1"
version: "1"
id: "cloudbee-ci-demo-ops-controller"
description: "CloudBees CI configuration bundle for the cloudbee-ci-demo ops-controller Controller"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"
